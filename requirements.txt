# Core: OSC, web UI
python-osc>=1.9
Flask>=3.0
Flask-SocketIO>=5.3

# Audio: notch filter bank, echo cancellation, FFT detection (app.audio)
sounddevice>=0.4
scipy>=1.11
numpy>=1.26

# ML: onnxruntime is for app.audio.ml.classifier's inference path once a
# trained model exists (Phase 5 -- needs real ring-out recordings first,
# not built yet). Nothing currently imports it at runtime.
onnxruntime>=1.17

# MIDI (app.midi)
mido>=1.3
# python-rtmidi only ships prebuilt wheels for Python 3.8-3.12 (win_amd64,
# manylinux, macOS). On a newer/older interpreter, pip falls back to a
# source build that needs a C++ compiler (MSVC/MinGW/clang) on PATH. If
# install fails here: either use Python 3.8-3.12 for this project, install
# a C++ compiler (Windows: "Build Tools for Visual Studio", C++ workload),
# or comment out this line and mido above and skip MIDI control surface
# support -- app.midi.devices.list_midi_input_ports() degrades gracefully
# without it, but app.midi.service.MidiService needs both.
python-rtmidi>=1.5
